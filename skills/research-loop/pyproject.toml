[project]
name = "research-loop-gate"
version = "0.1.0"
description = "Draft validation gates for the research-loop skill"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
research-loop-gate = "gates:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["gates"]
